% Generated by hand for v0.1; switch to roxygen2 generation once that
% dep is installed in the project's R lib.
\name{load_deeprv}
\alias{load_deeprv}
\title{Load a pre-trained DeepRV decoder}
\usage{
load_deeprv(domain, grid_size, kernel)
}
\arguments{
\item{domain}{Coordinate domain. \code{"unit_interval"} is the only
1D decoder shipped in v0.1; use \code{\link{load_deeprv_kron}} for
\code{"unit_square"}.}

\item{grid_size}{Number of grid points. Must equal one of the
sizes in the shipped catalog.}

\item{kernel}{Covariance kernel: \code{"matern_1_2"},
\code{"matern_3_2"}, \code{"matern_5_2"}, or \code{"rbf"}.}
}
\value{
A list with class \code{"deepRV_decoder"} and the schema
documented in \code{DESIGN.md} section 2.2. Accessors of interest:
\code{$grid_coords} (the exact training grid), \code{$ls_trained_range},
and \code{$weights}.
}
\description{
Looks up a decoder by \code{(domain, grid_size, kernel)} against
the shipped catalog manifest, validates its fingerprint, and returns
a \code{deepRV_decoder} object.
}
\details{
Decoder weights are MLP parameters of \code{MLPDeepRV(dims = [L, L])}
trained to emulate \code{chol(K_ls) \%*\% z} for length-scale
\code{ls} drawn from the decoder's \code{ls_trained_range}.
Inference outside that range is not supported.
}
\examples{
\dontrun{
dr <- load_deeprv("unit_interval", grid_size = 100, kernel = "matern_3_2")
print(dr)
}
}
